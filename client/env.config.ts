/**
 * Environment configuration
 *
 * ⚠️ ВАЖНО: Обновите эти значения для вашей локальной сети!
 *
 * Как узнать свой локальный IP адрес:
 * - Windows: откройте cmd и введите: ipconfig
 * - macOS/Linux: откройте терминал и введите: ifconfig
 *
 * Найдите IPv4 адрес вашего активного сетевого подключения
 * (обычно начинается с 192.168.x.x или 10.0.x.x)
 */

export const ENV_CONFIG = {
	/**
	 * Локальный IP адрес для генерации QR кода
	 *
	 * Замените на ваш текущий локальный IP адрес.
	 * Этот адрес будет использоваться для генерации QR кода,
	 * который будут сканировать мобильные устройства.
	 *
	 * @example '192.168.1.98'
	 * @example '10.0.0.5'
	 */
	LOCAL_IP: '192.168.1.43',

	/**
	 * Порт клиентского приложения (Next.js dev server)
	 *
	 * По умолчанию Next.js использует порт 3000.
	 */
	CLIENT_PORT: 3000,

	/**
	 * Порт сервера Socket.IO
	 *
	 * Сервер запускается на этом порту.
	 */
	SERVER_PORT: 3001,
};
